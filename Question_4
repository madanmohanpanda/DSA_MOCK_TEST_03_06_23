4. Create an interface called Drawable with a method draw() that has no implementation. Implement this interface in classes Circle and Rectangle. Write 
a program that creates objects of Circle and Rectangle and calls the draw() method on each object.

interface Drawable {
  void draw();
}

class Circle implements Drawable{
    pulic void draw(){
      System.out.println("Drawing Cricle...");
    }
}

class Rectangle implements Drawable{
    pulic void draw(){
      System.out.println("Drawing Rectangle...");
    }
}

class Main{
    public static void main(String [] a){
          Circle c = new Circle();
          Rectangle r = new Rectangle();
          c.draw();
          r.draw();
    }
}
